// Função Hash com Ponderação Dinâmica

// @Override
// protected int hashFunction(String key) {
//     int hash = 0;
//     for (int i = 0; i < key.length(); i++) {
//         hash += key.charAt(i) * (i + 1); // Multiplica o valor do caractere pela posição
//     }
//     return Math.abs(hash % size); // Garantir um índice dentro do tamanho da tabela
// }
