// Função Hash com Deslocamento de Bits

// @Override
// protected int hashFunction(String key) {
//     int hash = 0;
//     for (int i = 0; i < key.length(); i++) {
//         hash = (hash << 5) + key.charAt(i); // Deslocamento de bits à esquerda
//     }
//     return hash % size; // Retorna o índice após o módulo
// }
