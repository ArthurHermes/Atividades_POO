// Função Hash de "Rodízio" (Shift)

// @Override
// protected int hashFunction(String key) {
//     int hash = 0;
//     for (int i = 0; i < key.length(); i++) {
//         hash = (hash << 5) + key.charAt(i); // Desloca os bits à esquerda e soma o valor do caractere
//     }
//     return Math.abs(hash % size); // Módulo para garantir um índice válido
// }
