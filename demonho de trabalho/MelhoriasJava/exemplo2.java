// Função Hash baseada em XOR (Exclusivo OR)

// @Override
// protected int hashFunction(String key) {
//     int hash = 0;
//     for (int i = 0; i < key.length(); i++) {
//         hash ^= key.charAt(i); // XOR entre o valor atual do hash e o valor do caractere
//     }
//     return Math.abs(hash % size); // Módulo para garantir um índice válido
// }
